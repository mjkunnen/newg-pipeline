---
phase: 04-launch-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - decarba-remixer/src/launcher/meta.ts
  - decarba-remixer/src/launcher/fromPostgres.ts
  - decarba-remixer/src/lib/driveDownload.ts
  - decarba-remixer/src/launcher/fromSheet.ts
  - decarba-remixer/src/launcher/__tests__/fromPostgres.test.ts
  - decarba-remixer/package.json
  - decarba-remixer/.env.example
  - .github/workflows/launch-campaigns.yml
autonomous: true
requirements:
  - LAUNCH-01
must_haves:
  truths:
    - "Launcher reads ready_to_launch items from Postgres content API and launches them via Meta"
    - "Already-launched items are never re-launched (idempotent via status query)"
    - "If content API is unreachable, workflow falls back to Google Sheets launcher"
    - "Items missing drive_link in metadata_json are skipped with a warning"
    - "After successful launch, item status is advanced to launched in Postgres"
    - "Graph API calls use v23.0 (not expired v21.0)"
  artifacts:
    - path: "decarba-remixer/src/launcher/fromPostgres.ts"
      provides: "Postgres-driven launcher entrypoint"
      min_lines: 80
    - path: "decarba-remixer/src/lib/driveDownload.ts"
      provides: "Shared Drive download logic extracted from fromSheet.ts"
      exports: ["downloadCreative"]
    - path: "decarba-remixer/src/launcher/__tests__/fromPostgres.test.ts"
      provides: "Unit tests for extractMeta and fetchReadyItems"
      min_lines: 30
  key_links:
    - from: "decarba-remixer/src/launcher/fromPostgres.ts"
      to: "/api/content?status=ready_to_launch"
      via: "fetch with Bearer token"
      pattern: "fetch.*api/content.*ready_to_launch"
    - from: "decarba-remixer/src/launcher/fromPostgres.ts"
      to: "decarba-remixer/src/launcher/meta.ts"
      via: "import launchBatch"
      pattern: "import.*launchBatch.*meta"
    - from: "decarba-remixer/src/launcher/fromPostgres.ts"
      to: "/api/content/{id}/status"
      via: "PATCH fetch after launch success"
      pattern: "PATCH.*status.*launched"
    - from: ".github/workflows/launch-campaigns.yml"
      to: "npm run launch:postgres"
      via: "workflow step with continue-on-error fallback"
      pattern: "launch:postgres"
user_setup:
  - service: railway-content-api
    why: "Postgres launcher needs content API access"
    env_vars:
      - name: CONTENT_API_URL
        source: "Railway dashboard -> ad-command-center service -> Public URL"
      - name: DASHBOARD_SECRET
        source: "Same secret used for dashboard auth (already in Railway)"
    dashboard_config:
      - task: "Add CONTENT_API_URL and DASHBOARD_SECRET to GitHub Actions repository secrets"
        location: "GitHub -> Repository -> Settings -> Secrets -> Actions"
---

<objective>
Create the Postgres-driven launcher (`fromPostgres.ts`) that reads `ready_to_launch` items from the content API, launches them via Meta, and marks them `launched`. Upgrade Graph API from expired v21.0 to v23.0. Extract shared Drive download logic. Update workflow with Postgres-first + Sheets-fallback pattern.

Purpose: Replace Google Sheets as the primary launch data source with Postgres, making the launch pipeline consistent with the state layer built in Phases 1-3. Fix the dead Graph API version that currently blocks all Meta API calls.

Output: Working `fromPostgres.ts`, shared `driveDownload.ts`, updated `meta.ts` (v23.0), updated workflow with fallback, unit tests.
</objective>

<execution_context>
@C:/Users/maxku/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maxku/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-launch-hardening/04-CONTEXT.md
@.planning/phases/04-launch-hardening/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From decarba-remixer/src/launcher/meta.ts:
```typescript
export interface SubmissionInput {
  adId: string;
  adCopy: string;
  creativePath: string;
  creativeType: "image" | "video";
  landingPage: string;
  date: string;
  dailyBudget?: number;
}

export interface BatchResult {
  campaignId: string;
  adSetId: string;
  ads: { adId: string; adCreativeId: string; metaAdId: string }[];
}

export async function launchBatch(inputs: SubmissionInput[]): Promise<BatchResult>;
```

From decarba-remixer/src/lib/contentApi.ts:
```typescript
// Auth pattern — Bearer token with DASHBOARD_SECRET
const resp = await fetch(`${contentApiUrl}/api/content`, {
  headers: {
    "Content-Type": "application/json",
    Authorization: `Bearer ${dashboardSecret}`,
  },
});
```

From ad-command-center/routes/content.py:
```
GET  /api/content?status=ready_to_launch  -> list of ContentItem dicts
PATCH /api/content/{id}/status  -> body: {"status": "launched"}
```

From decarba-remixer/src/launcher/fromSheet.ts:
```typescript
// downloadCreative pattern — Drive share link -> direct download -> verify not HTML
async function downloadCreative(driveLink: string, adId: string): Promise<string>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Upgrade Graph API version + extract driveDownload + create fromPostgres.ts with tests</name>
  <files>
    decarba-remixer/src/launcher/meta.ts,
    decarba-remixer/src/lib/driveDownload.ts,
    decarba-remixer/src/launcher/fromSheet.ts,
    decarba-remixer/src/launcher/fromPostgres.ts,
    decarba-remixer/src/launcher/__tests__/fromPostgres.test.ts,
    decarba-remixer/package.json,
    decarba-remixer/.env.example
  </files>
  <read_first>
    decarba-remixer/src/launcher/meta.ts,
    decarba-remixer/src/launcher/fromSheet.ts,
    decarba-remixer/src/lib/contentApi.ts,
    decarba-remixer/package.json,
    decarba-remixer/.env.example,
    decarba-remixer/tsconfig.json
  </read_first>
  <behavior>
    - Test: extractMeta() returns {driveLink, landingPage} when metadata_json has drive_link
    - Test: extractMeta() returns null when metadata_json has no drive_link (per D-05)
    - Test: extractMeta() defaults landingPage to "https://newgarments.nl" when absent from metadata_json
    - Test: extractMeta() returns null for invalid JSON in metadata_json
    - Test: GRAPH_API constant in meta.ts is v23.0, not v21.0
  </behavior>
  <action>
    **Step 1 — Upgrade Graph API version (critical fix):**
    In `meta.ts` line 16, change:
    `const GRAPH_API = "https://graph.facebook.com/v21.0"` to
    `const GRAPH_API = "https://graph.facebook.com/v23.0"`
    This is the ONLY change to meta.ts in this task. v21.0 expired September 2025 — all Meta API calls are currently failing.

    **Step 2 — Extract shared driveDownload.ts:**
    Create `decarba-remixer/src/lib/driveDownload.ts` by extracting the `downloadCreative()` function from `fromSheet.ts` (lines 94-132). Export it as a named export. The function takes `(driveLink: string, adId: string, tmpDir: string)` and returns `Promise<string>` (file path). Move the Drive link regex, download logic, content-type detection, and HTML verification page check into this shared module.

    Update `fromSheet.ts` to import `downloadCreative` from `../lib/driveDownload.js` instead of having its own copy. Pass `TMP_DIR` as the third argument.

    **Step 3 — Create fromPostgres.ts (per D-01, D-02, D-03, D-04, D-05, D-13):**
    Create `decarba-remixer/src/launcher/fromPostgres.ts` with this structure:

    - Import `dotenv/config`, `writeFile/mkdir/unlink` from `fs/promises`, `join/extname` from `path`
    - Import `launchBatch` and `SubmissionInput` from `./meta.js`
    - Import `downloadCreative` from `../lib/driveDownload.js`
    - Read `--dry-run` flag: `const dryRun = process.argv.includes("--dry-run")` (wire used in Plan 02)
    - Define `ContentItem` interface matching the API response: `{id, content_id, source, status, creative_url, ad_copy, metadata_json}`
    - `fetchReadyItems()`: Use `process.env.CONTENT_API_URL` and `process.env.DASHBOARD_SECRET` (requireEnv pattern — throw if missing). GET `${contentApiUrl}/api/content?status=ready_to_launch` with `Authorization: Bearer ${dashboardSecret}`. Return parsed JSON array.
    - `extractMeta(item)`: Parse `item.metadata_json`, extract `drive_link` (required — skip item if missing per D-05). Extract `landing_page` (default to `"https://newgarments.nl"` if absent — per research finding that dashboard submit form doesn't write landing_page). Return `{driveLink, landingPage}` or `null`.
    - `markLaunched(itemId)`: PATCH `${contentApiUrl}/api/content/${itemId}/status` with `{"status": "launched"}` and Bearer auth. Per D-02.
    - `main()`: try/catch `fetchReadyItems()` — on failure `process.exit(1)` (triggers Sheets fallback in workflow per D-04). If zero items, log and `process.exit(0)` (no fallback needed). For each item: extractMeta (skip if null), downloadCreative, build SubmissionInput, call launchBatch. On success: markLaunched for each. On failure: log error, don't advance status (D-02). Cleanup temp files in finally block.

    **Step 4 — Update package.json:**
    Add script: `"launch:postgres": "node dist/launcher/fromPostgres.js"`

    **Step 5 — Update .env.example:**
    Add `CONTENT_API_URL=` and `DASHBOARD_SECRET=` if not already present. Add `APPS_SCRIPT_URL=` (for fromSheet.ts cleanup — move hardcoded URL to env var per CONTEXT.md specifics).

    **Step 6 — Move APPS_SCRIPT_URL to env var in fromSheet.ts:**
    Replace the hardcoded `APPS_SCRIPT_URL` constant (line 6-7) with `const APPS_SCRIPT_URL = process.env.APPS_SCRIPT_URL || ""`. Add a check in `updateSheetStatus`: if `!APPS_SCRIPT_URL`, log warning and return early.
  </action>
  <verify>
    <automated>cd decarba-remixer && npm run build && npm test -- --reporter=verbose 2>&1 | head -80</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "v23.0" decarba-remixer/src/launcher/meta.ts (Graph API upgraded from v21.0)
    - grep -q "fetchReadyItems" decarba-remixer/src/launcher/fromPostgres.ts (Postgres read function exists)
    - grep -q "markLaunched" decarba-remixer/src/launcher/fromPostgres.ts (status write-back exists)
    - grep -q "extractMeta" decarba-remixer/src/launcher/fromPostgres.ts (metadata extraction exists)
    - grep -q "newgarments.nl" decarba-remixer/src/launcher/fromPostgres.ts (landing_page default)
    - grep -q "process.exit(1)" decarba-remixer/src/launcher/fromPostgres.ts (fallback trigger on API failure)
    - grep -q "downloadCreative" decarba-remixer/src/lib/driveDownload.ts (shared module exists)
    - grep -q "launch:postgres" decarba-remixer/package.json (npm script added)
    - grep -q "CONTENT_API_URL" decarba-remixer/.env.example (env var documented)
    - grep -q "extractMeta" decarba-remixer/src/launcher/__tests__/fromPostgres.test.ts (tests exist)
  </acceptance_criteria>
  <done>
    - Graph API upgraded to v23.0 in meta.ts
    - fromPostgres.ts reads from content API, extracts metadata, downloads creatives, calls launchBatch, marks launched on success
    - downloadCreative() extracted to shared lib/driveDownload.ts, used by both fromSheet.ts and fromPostgres.ts
    - Items missing drive_link are skipped with warning (D-05)
    - landing_page defaults to https://newgarments.nl when absent from metadata_json
    - Content API unreachable -> exit(1) triggers Sheets fallback (D-04)
    - Zero items -> exit(0), no fallback (D-04)
    - package.json has launch:postgres script (D-03)
    - .env.example updated with CONTENT_API_URL, DASHBOARD_SECRET, APPS_SCRIPT_URL
    - Unit tests pass for extractMeta edge cases
    - TypeScript compiles cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: Update launch-campaigns.yml with Postgres-first fallback and new secrets</name>
  <files>
    .github/workflows/launch-campaigns.yml
  </files>
  <read_first>
    .github/workflows/launch-campaigns.yml
  </read_first>
  <action>
    Update `.github/workflows/launch-campaigns.yml` per D-03, D-04:

    **1. Add new secrets to the .env creation step:**
    Add these lines to the `cat > .env` block:
    ```
    CONTENT_API_URL=${{ secrets.CONTENT_API_URL }}
    DASHBOARD_SECRET=${{ secrets.DASHBOARD_SECRET }}
    APPS_SCRIPT_URL=${{ secrets.APPS_SCRIPT_URL }}
    ```

    **2. Replace the single launch step with Postgres-first + Sheets-fallback:**
    Replace the existing "Launch pending campaigns" step with:

    ```yaml
    - name: Launch from Postgres
      id: postgres_launch
      run: npm run launch:postgres
      timeout-minutes: 5
      continue-on-error: true

    - name: Launch from Sheets (fallback)
      if: steps.postgres_launch.outcome == 'failure'
      run: npm run launch
      timeout-minutes: 5
    ```

    Per D-04: `fromPostgres.ts` exits with code 1 when content API is unreachable (triggers fallback). Exits with code 0 when zero items found (no fallback — nothing to launch). The `continue-on-error: true` + checking `outcome == 'failure'` implements this correctly.

    Do NOT add dry-run support to the workflow yet — that is Plan 02.
  </action>
  <verify>
    <automated>grep -c "postgres_launch\|launch:postgres\|CONTENT_API_URL\|DASHBOARD_SECRET" .github/workflows/launch-campaigns.yml</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "launch:postgres" .github/workflows/launch-campaigns.yml (Postgres launcher step exists)
    - grep -q "continue-on-error: true" .github/workflows/launch-campaigns.yml (fallback pattern)
    - grep -q "steps.postgres_launch.outcome" .github/workflows/launch-campaigns.yml (conditional fallback)
    - grep -q "CONTENT_API_URL" .github/workflows/launch-campaigns.yml (secret wired)
    - grep -q "DASHBOARD_SECRET" .github/workflows/launch-campaigns.yml (secret wired)
    - grep -q "fromSheet\|launch$" .github/workflows/launch-campaigns.yml (Sheets fallback preserved)
  </acceptance_criteria>
  <done>
    - Workflow runs launch:postgres first with continue-on-error
    - On failure (exit 1 = API unreachable), falls back to npm run launch (Sheets)
    - On success (exit 0 = launched or zero items), no fallback triggered
    - CONTENT_API_URL and DASHBOARD_SECRET secrets wired into .env
    - APPS_SCRIPT_URL secret wired for fromSheet.ts
    - Existing Sheets-based launch preserved as fallback (D-04)
  </done>
</task>

</tasks>

<verification>
1. `cd decarba-remixer && npm run build` — TypeScript compiles without errors
2. `cd decarba-remixer && npm test` — all tests pass including new fromPostgres tests
3. `grep "v23.0" decarba-remixer/src/launcher/meta.ts` — Graph API version upgraded
4. `grep "launch:postgres" decarba-remixer/package.json` — npm script exists
5. `grep "postgres_launch" .github/workflows/launch-campaigns.yml` — workflow has fallback pattern
</verification>

<success_criteria>
- fromPostgres.ts is a complete launcher that reads from content API, downloads creatives, launches via Meta, and updates status
- Graph API upgraded from dead v21.0 to v23.0
- downloadCreative() shared between both launchers (DRY)
- Workflow runs Postgres-first with Sheets fallback on API failure
- Unit tests cover extractMeta edge cases (missing drive_link, missing landing_page, invalid JSON)
- All env vars documented in .env.example
</success_criteria>

<output>
After completion, create `.planning/phases/04-launch-hardening/04-01-SUMMARY.md`
</output>
