---
phase: 02-discovery-reliability
plan: 05
type: execute
wave: 2
depends_on: ["02-02", "02-03", "02-04"]
files_modified:
  - decarba-remixer/src/scraper/ppspy.ts
  - decarba-remixer/config/ppspy-settings.json
  - .github/workflows/daily-scrape.yml
  - .github/workflows/daily-pinterest.yml
  - decarba-remixer/src/scraper/__tests__/ppspy-config.test.ts
  - scout/config.py
autonomous: true
requirements:
  - SRC-04
  - DISC-04
  - DISC-05

user_setup:
  - service: slack
    why: "Failure alerting for scraping workflows"
    env_vars:
      - name: SLACK_WEBHOOK_URL
        source: "Slack App → Incoming Webhooks → Create new webhook → Copy URL. Add as GitHub Actions secret: repo Settings → Secrets → New repository secret → name: SLACK_WEBHOOK_URL"

must_haves:
  truths:
    - "PPSpy search term comes from config/ppspy-settings.json, not hardcoded in ppspy.ts"
    - "daily-scrape.yml includes Meta Ad Library as a scraping step"
    - "Workflow failures trigger a Slack notification via SLACK_WEBHOOK_URL"
    - "Each scraper step writes structured results to GITHUB_STEP_SUMMARY"
    - "git add step no longer references scout/processed_tiktok.json"
    - "daily-pinterest.yml checks Postgres for seen pins before running cloud_pinterest.py per D-07"
    - "scout/config.py is archived per D-10"
  artifacts:
    - path: "decarba-remixer/config/ppspy-settings.json"
      provides: "PPSpy scraper configuration"
      contains: "search_terms"
    - path: ".github/workflows/daily-scrape.yml"
      provides: "Updated workflow with Meta step + Slack alerts"
      contains: "scrape:meta"
    - path: ".github/workflows/daily-pinterest.yml"
      provides: "Pinterest remake workflow with Postgres dedup check"
      contains: "CONTENT_API_URL"
  key_links:
    - from: ".github/workflows/daily-scrape.yml"
      to: "SLACK_WEBHOOK_URL secret"
      via: "curl POST in if: failure() step"
      pattern: "SLACK_WEBHOOK_URL"
    - from: ".github/workflows/daily-scrape.yml"
      to: "GITHUB_STEP_SUMMARY"
      via: "echo >> $GITHUB_STEP_SUMMARY"
      pattern: "GITHUB_STEP_SUMMARY"
    - from: ".github/workflows/daily-pinterest.yml"
      to: "Content API GET /api/content"
      via: "curl check before cloud_pinterest.py"
      pattern: "CONTENT_API_URL.*api/content"
---

<objective>
Harden PPSpy with config externalization, add Meta Ad Library step to daily-scrape.yml, add structured logging + Slack failure alerts to all workflow steps, fix the commit step that still references scout/processed_tiktok.json, add Postgres dedup check to daily-pinterest.yml per D-07, and archive scout/config.py per D-10.

Purpose: All four sources run in daily-scrape.yml with structured output and failure visibility (DISC-04), PPSpy config is externalized (SRC-04, DISC-05), daily-pinterest.yml checks Postgres for seen content (D-07), and legacy config is retired (D-10).
Output: Updated ppspy.ts, ppspy-settings.json, updated daily-scrape.yml and daily-pinterest.yml, archived legacy files.
</objective>

<execution_context>
@C:\Users\maxku\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\maxku\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-discovery-reliability/02-CONTEXT.md
@.planning/phases/02-discovery-reliability/02-RESEARCH.md
@.planning/phases/02-discovery-reliability/02-02-SUMMARY.md
@.planning/phases/02-discovery-reliability/02-03-SUMMARY.md
@.planning/phases/02-discovery-reliability/02-04-SUMMARY.md

@decarba-remixer/src/scraper/ppspy.ts
@.github/workflows/daily-scrape.yml
@.github/workflows/daily-pinterest.yml
@pipeline/cloud_pinterest.py

<interfaces>
<!-- From config.ts (plan 01) -->
export function loadConfig<T>(filename: string): T;

<!-- Structured result line format (from RESEARCH.md) -->
// Each scraper emits: [result] source=X found=N written=N skipped=N errors=N
// Workflow parses this and appends to $GITHUB_STEP_SUMMARY

<!-- Slack webhook payload format -->
// POST to $SLACK_WEBHOOK_URL with {"text": "message"}

<!-- Content API GET endpoint (from ad-command-center/routes/content.py) -->
// GET /api/content?source=pinterest returns list of content items
// Each item has content_id field — used for dedup check
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Externalize PPSpy search term to config + add structured output</name>
  <files>
    decarba-remixer/src/scraper/ppspy.ts
    decarba-remixer/config/ppspy-settings.json
    decarba-remixer/src/scraper/__tests__/ppspy-config.test.ts
  </files>
  <read_first>
    decarba-remixer/src/scraper/ppspy.ts
    decarba-remixer/src/scraper/config.ts
    decarba-remixer/src/scraper/contentApi.ts
    decarba-remixer/src/scraper/__tests__/ppspy-config.test.ts
  </read_first>
  <action>
1. Create `decarba-remixer/config/ppspy-settings.json`:
   ```json
   {
     "search_terms": ["decarba"],
     "ad_forecast_filter": [3],
     "order_by": "ad_created_at",
     "direction": "desc"
   }
   ```

2. In `ppspy.ts`:
   - Add import: `import { loadConfig } from "./config.js";`
   - Add interface:
     ```typescript
     interface PPSpyConfig {
       search_terms: string[];
       ad_forecast_filter: number[];
       order_by: string;
       direction: string;
     }
     ```
   - Replace the hardcoded `PPSPY_ADS_URL` constant (line 21) with a function that builds the URL from config:
     ```typescript
     function buildPPSpyUrl(config: PPSpyConfig): string {
       const keywords = config.search_terms.map(term => ({
         field: "all",
         value: term,
         logic_operator: "and"
       }));
       return `https://app.ppspy.com/ads?extend_keywords=${encodeURIComponent(JSON.stringify(keywords))}&ad_forecast=${JSON.stringify(config.ad_forecast_filter)}&order_by=${config.order_by}&direction=${config.direction}`;
     }
     ```
   - Load config at start of `scrapePPSpy()`: `const config = loadConfig<PPSpyConfig>("ppspy-settings.json");`
   - Use `buildPPSpyUrl(config)` instead of `PPSPY_ADS_URL`

3. At the end of `scrapePPSpy()`, after writing metadata, add structured result logging:
   ```typescript
   console.log(`[result] source=ppspy found=${ads.length} written=${ads.length} skipped=0 errors=0`);
   ```
   (PPSpy already writes to content API via index.ts calling writeToContentAPI, so the "written" count here is just the found count — the actual write happens in index.ts. This structured line is for the workflow to parse.)

4. Update `ppspy-config.test.ts` with a real test:
   ```typescript
   import { describe, it, expect } from "vitest";
   import { loadConfig } from "../config.js";

   describe("PPSpy config loading", () => {
     it("reads search_terms from ppspy-settings.json", () => {
       const config = loadConfig<{ search_terms: string[] }>("ppspy-settings.json");
       expect(config.search_terms).toContain("decarba");
       expect(Array.isArray(config.search_terms)).toBe(true);
     });
   });
   ```
  </action>
  <verify>
    <automated>cd decarba-remixer && npx tsc --noEmit && npx vitest run src/scraper/__tests__/ppspy-config.test.ts --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - decarba-remixer/config/ppspy-settings.json contains "search_terms": ["decarba"]
    - decarba-remixer/src/scraper/ppspy.ts contains `import { loadConfig } from "./config.js"`
    - decarba-remixer/src/scraper/ppspy.ts contains `buildPPSpyUrl(`
    - decarba-remixer/src/scraper/ppspy.ts does NOT contain `const PPSPY_ADS_URL =` as a static string with "decarba" hardcoded
    - decarba-remixer/src/scraper/ppspy.ts contains `[result] source=ppspy`
    - `cd decarba-remixer && npx tsc --noEmit` exits 0
    - `cd decarba-remixer && npx vitest run src/scraper/__tests__/ppspy-config.test.ts` exits 0
  </acceptance_criteria>
  <done>PPSpy search term externalized to config, structured result output added, config test passes</done>
</task>

<task type="auto">
  <name>Task 2: Add Meta step + structured logging + Slack alerts + Postgres dedup for daily-pinterest.yml + fix commit in daily-scrape.yml</name>
  <files>
    .github/workflows/daily-scrape.yml
    .github/workflows/daily-pinterest.yml
  </files>
  <read_first>
    .github/workflows/daily-scrape.yml
    .github/workflows/daily-pinterest.yml
    pipeline/cloud_pinterest.py
  </read_first>
  <action>
1. Update `.github/workflows/daily-scrape.yml`:

   a. Add `APIFY_TOKEN` to the .env creation step (it already exists in the template but verify it's there for the Meta scraper).

   b. Add Meta Ad Library scraping step after the TikTok step:
      ```yaml
      - name: Scrape Meta Ad Library
        if: steps.check.outputs.already_ran == 'false'
        run: |
          OUTPUT=$(npm run scrape:meta 2>&1) || true
          echo "$OUTPUT"
          RESULT=$(echo "$OUTPUT" | grep "^\[result\]" | tail -1)
          if [ -n "$RESULT" ]; then
            FOUND=$(echo "$RESULT" | grep -oP 'found=\K\d+' || echo "0")
            WRITTEN=$(echo "$RESULT" | grep -oP 'written=\K\d+' || echo "0")
            SKIPPED=$(echo "$RESULT" | grep -oP 'skipped=\K\d+' || echo "0")
            ERRORS=$(echo "$RESULT" | grep -oP 'errors=\K\d+' || echo "0")
            echo "| Meta | $FOUND | $WRITTEN | $SKIPPED | $ERRORS |" >> $GITHUB_STEP_SUMMARY
          fi
        timeout-minutes: 8
      ```

   c. Wrap existing PPSpy, Pinterest, and TikTok steps with the same structured output parsing. For each step, change the `run` to capture output and parse `[result]` lines. Example for PPSpy:
      ```yaml
      - name: Scrape PPSpy
        if: steps.check.outputs.already_ran == 'false'
        run: |
          OUTPUT=$(npm run scrape 2>&1) || true
          echo "$OUTPUT"
          RESULT=$(echo "$OUTPUT" | grep "^\[result\]" | tail -1)
          if [ -n "$RESULT" ]; then
            FOUND=$(echo "$RESULT" | grep -oP 'found=\K\d+' || echo "0")
            WRITTEN=$(echo "$RESULT" | grep -oP 'written=\K\d+' || echo "0")
            SKIPPED=$(echo "$RESULT" | grep -oP 'skipped=\K\d+' || echo "0")
            ERRORS=$(echo "$RESULT" | grep -oP 'errors=\K\d+' || echo "0")
            echo "| PPSpy | $FOUND | $WRITTEN | $SKIPPED | $ERRORS |" >> $GITHUB_STEP_SUMMARY
          fi
        timeout-minutes: 10
      ```
      Same pattern for Pinterest (timeout 5) and TikTok (timeout 12).

   d. Add a GITHUB_STEP_SUMMARY header step before the scraping steps:
      ```yaml
      - name: Initialize job summary
        if: steps.check.outputs.already_ran == 'false'
        run: |
          echo "## Scrape Results $(date +%Y-%m-%d)" >> $GITHUB_STEP_SUMMARY
          echo "| Source | Found | Written | Skipped | Errors |" >> $GITHUB_STEP_SUMMARY
          echo "|--------|-------|---------|---------|--------|" >> $GITHUB_STEP_SUMMARY
      ```

   e. Fix the commit step (line 105): change `git add decarba-remixer/docs/ scout/processed_tiktok.json` to `git add decarba-remixer/docs/` — remove the `scout/processed_tiktok.json` reference entirely per D-02 (file-based dedup is retired).

   f. Add Slack failure notification as the LAST step in the `scrape-and-deploy` job:
      ```yaml
      - name: Notify Slack on failure
        if: failure()
        run: |
          if [ -n "${{ secrets.SLACK_WEBHOOK_URL }}" ]; then
            curl -s -X POST "${{ secrets.SLACK_WEBHOOK_URL }}" \
              -H "Content-Type: application/json" \
              -d "{\"text\": \":red_circle: *${{ github.workflow }}* failed on ${{ github.ref_name }} — <${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|View run>\"}"
          fi
      ```

2. Update `.github/workflows/daily-pinterest.yml` to check Postgres for seen pins per D-07:

   a. Add `CONTENT_API_URL` and `DASHBOARD_SECRET` to the env block of the job:
      ```yaml
      env:
        FAL_KEY: ${{ secrets.FAL_KEY }}
        PINTEREST_APPS_SCRIPT_URL: ${{ secrets.PINTEREST_APPS_SCRIPT_URL }}
        CONTENT_API_URL: ${{ secrets.CONTENT_API_URL }}
        DASHBOARD_SECRET: ${{ secrets.DASHBOARD_SECRET }}
      ```

   b. Add a new step BEFORE "Run Pinterest remake pipeline" that queries Postgres for already-processed pinterest pin IDs and writes them to a file that cloud_pinterest.py can read:
      ```yaml
      - name: Fetch processed pin IDs from Postgres
        if: steps.check.outputs.already_ran == 'false'
        run: |
          if [ -n "$CONTENT_API_URL" ] && [ -n "$DASHBOARD_SECRET" ]; then
            echo "Fetching processed pinterest pins from content API..."
            curl -sf "${CONTENT_API_URL}/api/content?source=pinterest" \
              -H "Authorization: Bearer ${DASHBOARD_SECRET}" \
              | python3 -c "import sys,json; items=json.load(sys.stdin); print('\n'.join(i['content_id'] for i in items))" \
              > /tmp/processed_pin_ids.txt 2>/dev/null || echo "Warning: Could not fetch processed pins from Postgres"
            echo "Found $(wc -l < /tmp/processed_pin_ids.txt) already-processed pins in Postgres"
          else
            echo "CONTENT_API_URL not set — skipping Postgres dedup pre-check"
            touch /tmp/processed_pin_ids.txt
          fi
      ```

   c. Update the "Run Pinterest remake pipeline" step to pass the processed IDs file:
      ```yaml
      - name: Run Pinterest remake pipeline
        if: steps.check.outputs.already_ran == 'false'
        env:
          FAL_KEY: ${{ secrets.FAL_KEY }}
          PINTEREST_APPS_SCRIPT_URL: ${{ secrets.PINTEREST_APPS_SCRIPT_URL }}
          PROCESSED_PINS_FILE: /tmp/processed_pin_ids.txt
        run: python pipeline/cloud_pinterest.py --max 2
      ```

   d. In `pipeline/cloud_pinterest.py`, update the `get_processed_pin_ids()` function to ALSO read from the `PROCESSED_PINS_FILE` env var (Postgres-sourced IDs) and merge them with the Google Sheet IDs. This ensures Postgres is the primary dedup source per D-07, while the Sheet serves as fallback:
      ```python
      # At the top of get_processed_pin_ids(), BEFORE reading the sheet:
      postgres_file = os.getenv("PROCESSED_PINS_FILE")
      if postgres_file and os.path.exists(postgres_file):
          with open(postgres_file) as f:
              for line in f:
                  pid = line.strip()
                  if pid:
                      pin_ids.add(pid)
          log.info(f"Postgres: {len(pin_ids)} pre-loaded pin IDs from content API")
      ```
      Add this block at the start of the function, initializing `pin_ids = set()` before the block, then let the existing Sheet CSV logic add to the same set. This way Postgres IDs are checked first, and the Sheet provides any IDs that haven't been migrated yet.

   e. Add the same Slack failure notification step at the end of the daily-pinterest.yml job:
      ```yaml
      - name: Notify Slack on failure
        if: failure()
        run: |
          if [ -n "${{ secrets.SLACK_WEBHOOK_URL }}" ]; then
            curl -s -X POST "${{ secrets.SLACK_WEBHOOK_URL }}" \
              -H "Content-Type: application/json" \
              -d "{\"text\": \":red_circle: *${{ github.workflow }}* failed on ${{ github.ref_name }} — <${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|View run>\"}"
          fi
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
      ```
  </action>
  <verify>
    <automated>grep -c "SLACK_WEBHOOK_URL" .github/workflows/daily-scrape.yml && grep -c "scrape:meta" .github/workflows/daily-scrape.yml && grep -c "processed_tiktok" .github/workflows/daily-scrape.yml && grep -c "CONTENT_API_URL" .github/workflows/daily-pinterest.yml && grep -c "processed_pin_ids" .github/workflows/daily-pinterest.yml</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/daily-scrape.yml contains `npm run scrape:meta`
    - .github/workflows/daily-scrape.yml contains `SLACK_WEBHOOK_URL`
    - .github/workflows/daily-scrape.yml contains `if: failure()`
    - .github/workflows/daily-scrape.yml contains `GITHUB_STEP_SUMMARY`
    - .github/workflows/daily-scrape.yml contains `## Scrape Results`
    - .github/workflows/daily-scrape.yml does NOT contain `scout/processed_tiktok.json`
    - .github/workflows/daily-scrape.yml contains `Scrape Meta Ad Library`
    - .github/workflows/daily-pinterest.yml contains `SLACK_WEBHOOK_URL`
    - .github/workflows/daily-pinterest.yml contains `CONTENT_API_URL`
    - .github/workflows/daily-pinterest.yml contains `processed_pin_ids.txt` (Postgres dedup pre-check)
    - pipeline/cloud_pinterest.py contains `PROCESSED_PINS_FILE` (reads Postgres-sourced IDs)
    - grep -c "processed_tiktok" .github/workflows/daily-scrape.yml returns 0
  </acceptance_criteria>
  <done>Workflow includes all four sources, structured logging to job summary, Slack failure alerts, daily-pinterest.yml checks Postgres for seen pins per D-07, no more scout/processed_tiktok.json in commit step</done>
</task>

<task type="auto">
  <name>Task 3: Archive legacy scripts per D-05, D-06, and D-10</name>
  <files>
    scout/tiktok_checker.py
    pipeline/cloud_pinterest.py
    scout/config.py
  </files>
  <read_first>
    scout/tiktok_checker.py
    pipeline/cloud_pinterest.py
    scout/config.py
  </read_first>
  <action>
1. Move `scout/tiktok_checker.py` to `archive/scout/tiktok_checker.py` using `git mv`:
   ```bash
   mkdir -p archive/scout
   git mv scout/tiktok_checker.py archive/scout/tiktok_checker.py
   ```
   Per D-05: incompatible dedup format, uses Apify not EnsembleData, canonical tiktok.ts is already automated.

2. Move `pipeline/cloud_pinterest.py` to `archive/pipeline/cloud_pinterest.py` using `git mv`:
   ```bash
   mkdir -p archive/pipeline
   git mv pipeline/cloud_pinterest.py archive/pipeline/cloud_pinterest.py
   ```
   Per D-06: scraping replaced by canonical pinterest.ts. The fal.ai remake logic may be reused later but is not needed for Phase 2.

3. Move `scout/config.py` to `archive/scout/config.py` using `git mv`:
   ```bash
   git mv scout/config.py archive/scout/config.py
   ```
   Per D-10: contains subscription box brand search terms (wrong domain). NEWGARMENTS competitor brands are now in `decarba-remixer/config/meta-competitors.json` (created by plan 02-04).

4. If `scout/processed_tiktok.json` still exists in the repo, move it to archive:
   ```bash
   git mv scout/processed_tiktok.json archive/scout/processed_tiktok.json 2>/dev/null || true
   ```

Note: Use `git mv` (not plain mv) to preserve git history. If the archive/ directory already exists from Phase 0, files go into appropriate subdirectories.

IMPORTANT: Task 2 modifies pipeline/cloud_pinterest.py (adds Postgres dedup). This task archives it AFTER Task 2 completes. The archived version will include the Postgres dedup changes — the archive preserves the final state. If daily-pinterest.yml later needs the remake logic, it can be extracted from the archive.
  </action>
  <verify>
    <automated>test ! -f scout/tiktok_checker.py && test ! -f pipeline/cloud_pinterest.py && test ! -f scout/config.py && echo "ARCHIVED" || echo "STILL EXISTS"</automated>
  </verify>
  <acceptance_criteria>
    - scout/tiktok_checker.py does NOT exist in active codebase
    - pipeline/cloud_pinterest.py does NOT exist in active codebase
    - scout/config.py does NOT exist in active codebase
    - archive/scout/tiktok_checker.py exists
    - archive/pipeline/cloud_pinterest.py exists
    - archive/scout/config.py exists
  </acceptance_criteria>
  <done>Legacy TikTok and Pinterest Python scrapers plus scout/config.py archived, canonical TypeScript scrapers and per-source JSON configs are the only active implementations</done>
</task>

</tasks>

<verification>
1. `cd decarba-remixer && npx vitest run` — all tests pass
2. `cd decarba-remixer && npx tsc --noEmit` — no TypeScript errors
3. `grep "processed_tiktok" .github/workflows/daily-scrape.yml` — returns nothing
4. `grep "scrape:meta" .github/workflows/daily-scrape.yml` — found
5. `grep "SLACK_WEBHOOK_URL" .github/workflows/daily-scrape.yml` — found
6. `grep "CONTENT_API_URL" .github/workflows/daily-pinterest.yml` — found
7. `grep "processed_pin_ids" .github/workflows/daily-pinterest.yml` — found
8. `test ! -f scout/tiktok_checker.py` — true
9. `test ! -f scout/config.py` — true
</verification>

<success_criteria>
- PPSpy search term from config (per D-14, SRC-04, DISC-05)
- daily-scrape.yml has Meta Ad Library step (per D-09, SRC-03)
- All workflow steps have structured GITHUB_STEP_SUMMARY output (per D-12)
- Slack failure alerts on all workflows (per D-11, DISC-04)
- Commit step no longer references retired dedup file
- daily-pinterest.yml checks Postgres for seen pins before running remake pipeline (per D-07)
- Legacy Python scrapers and scout/config.py archived (per D-05, D-06, D-10)
</success_criteria>

<output>
After completion, create `.planning/phases/02-discovery-reliability/02-05-SUMMARY.md`
</output>
